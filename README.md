# Suno-Playlist-Downloader-with-WAV-Support
GUI downloader for Suno playlists/clips that grabs MP3s plus artwork, lyrics, genres, and prompt text, and builds XLSX indexes (per-playlist + master). Optional Playwright + Chrome automation downloads WAV to the same folders and applies matching tags. Includes a PyInstaller EXE build line.
